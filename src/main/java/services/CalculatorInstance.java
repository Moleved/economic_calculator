package com.calculator.services;

public class CalculatorInstance extends Calculator {
    public double calculateDivision(double numerator, double denominator) {
        return numerator / denominator;
    }
}
